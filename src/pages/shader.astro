---
import Experience from "../components/experience.astro";
import Layout from "../layouts/BaseLayout.astro";
---

<Layout>
  <div class="webgl-wrapper">
    <Experience />
  </div>
  <div class="text-content">
    <div class="bio">
      <h2>
        ⇨ Sling partners with meaningful startups and impassioned individuals 🗩,
        reliably developing digital solutions and compelling online experiences.
        🌎
      </h2>
    </div>
    <div class="sub-bio">
      <h2 class="">
        We believe in focusing on relationships that foster meaningful work and
        giving projects the careful attention they deserve. <br />
        <br />
        In a sea of online content, we strive to create work that is
        thoughtful, intuitive and lasting. This means taking on projects we
        believe in, for people we care about.
      </h2>
      <span class="flag"> ⚐</span>
    </div>
  </div>
</Layout>

<style>
  .webgl-wrapper {
    height: 90vh;
    position: fixed;
    top: 0;
    border-radius: 40px;
  }
  
  .sling-header {
    font-size: 400px;
    letter-spacing: -1rem;
    font-family: "arial-narrow";
    display: flex;
    align-items: center;
    justify-content: center;
    /* position: relative; */
    position: sticky;
    top: 20vh;
    width: 100%;
    margin: 0 auto;
    padding-top: 10vh;
    text-align: center;
    /* outline: 1px solid; */
  }
  .char {
    display: block;
    transform-origin: bottom center;
  }

  .text-content {
    margin-top: 90vh;
    padding-bottom: 10rem;
    background: var(--grey-400);
    backdrop-filter: blur(40px);
    color: var(--white);
    position: relative;
    width: 100%;
    border-top: 3px solid black;
  }
  .bio {
    position: relative;
    width: 90%;
    margin: 10rem auto;
  }

  .bio > h2 {
    font-family: "items-thin";
    font-size: 80px;
    width: 90%;
    margin: 0 auto;
  }

  .bio > h2::first-letter {
    margin-left: 4rem;
  }

  .sub-bio {
    width: 63%;
    margin: 10rem auto;
    font-size: 40px;
    font-family: "items-thin";

    h2 {
      font-family: "items-thin";
      font-size: 40px;
      &:first-letter {
        margin-left: 4rem;
      }
    }

    .flag {
      margin: 5rem auto;
      display: inline-block;
      width: 100%;
      text-align: center;
      font-size: 80px;
    }
  }
  .sub-bio > h4::first-letter {
    margin-left: 4rem;
  }
</style>

<script>
  // import { animate, scroll } from "motion";

  // const char = document.querySelectorAll(".char");
  // // const header = document.querySelector('.sling-header')
  // const scrollArea = document.querySelector(".section-1");

  // char.forEach((ch, i) => {
  //   const index = i + 1;

  //   const transform = `translateY(-${index * 0 + 0}vh)`;
  //   const scale = `scaleY(${index * 0 + 3})`;

  //   scroll(
  //     animate(char[i], {
  //       transform: `${transform} ${scale}`,
  //     }),
  //     {
  //       target: scrollArea,
  //       offset: ["start start", "end start"],
  //     }
  //   );
  // });
</script>
